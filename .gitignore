# Build output
chatbox

# Runtime state — NEVER commit these.
# chatbox.token is the shared secret; chatbox.sqlite* holds message history.
chatbox.token
chatbox.sqlite
chatbox.sqlite-*
chatbox.log
*.bak-*
*.sqlite.bak-*

# Test scratch — throwaway server state written by tests/protocol.sh
tests/.scratch/

# macOS
.DS_Store
